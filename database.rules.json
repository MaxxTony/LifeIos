{
  "rules": {
    "focusRoom": {
      ".read": "auth != null",
      "$uid": {
        ".write": "auth != null && auth.uid === $uid",
        ".validate": "newData.hasChildren(['userName', 'lastActive', 'status', 'isPublic']) && newData.child('isPublic').val() === true"
      }
    }
  }
}
